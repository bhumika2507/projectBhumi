const add= (a,b) =>
{
    return a+b;
};

const sub= (a,b) =>
{
    return a-b;
};

const mul= (a,b) =>
{
    return a*b;
};

const Name="bhumi";

module.exports={add,sub,mul,Name};
// module.exports.sub=sub;
// module.exports.mul=mul;